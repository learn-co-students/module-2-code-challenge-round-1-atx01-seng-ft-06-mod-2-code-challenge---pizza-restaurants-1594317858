<%= form_for @pizza do |f|
    <%= f.label :name %> 
    <%= f.text_field :name %>


<%= f.label :ingredients %> 
<%= f.text_field :ingredients %>


<%= f.collection_check_boxes(:restaurant_ids, Restaraurant.all, :id, :name %>


<%= f.submit %>

<% end %>